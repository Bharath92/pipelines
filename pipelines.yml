pipelines:
  - name: test
    steps:
      - name: test
        type: PowerShell
        configuration:
          runtime:
            type: host
          nodePool: win_2019
          integrations:
            - name: s_artifactory
        execution:
          onExecute:
            - docker ps
            - cleanup_jfrog_cli --server-name s_artifactory
